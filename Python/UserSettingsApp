#enable logging module
import pdb
#Adding new settings
def add_setting(dictionary, tuple_pair):
    key,value=tuple_pair
    key=key.lower()
    value=value.lower()
    if key in dictionary.keys():
        
        return f"Setting '{key}' already exists! Cannot add a new setting with this name."
    else:
        dictionary[key]=value
        return f"Setting '{key}' added with value '{value}' successfully!"    
    
    return value

# Defining test dictionary with settings
test_settings={
"Theme": "Dark",
"Notifications": "enabled",
"Volume": "high"

}

#Update function

def update_setting(dictionary,pair):
    key,value=pair
    key=key.lower()
    value=value.lower()
    if key in dictionary.keys():
        dictionary[key]=value
        return f"Setting '{key}' updated to '{value}' successfully!"
    else:
        return f"Setting '{key}' does not exist! Cannot update a non-existing setting."


#Delete function
def delete_setting(dictionary,key):
    
    key=key.lower()
    if key in dictionary:
        del dictionary[key]
        return f"Setting '{key}' deleted successfully!"
    else:
        return f"Setting not found!"


#View function
def view_settings(dictionary):
    if len(dictionary) == 0:
        return "No settings available."
    else:
        settings="Current User Settings:\n"
        for key,value in dictionary.items():
            settings=settings+str(key.capitalize()+": "+value+"\n")
        return settings    

#Define empty dictionary
dictionary=()
pdb.set_trace()
tuple_pair= ("Color","GreEn") 
tuple_pair2= ("Theme","light") 
print(add_setting(test_settings,tuple_pair))
print(add_setting(test_settings,tuple_pair2))
print(add_setting(test_settings,tuple_pair2))
print(add_setting({'theme': 'light'}, ('volume', 'high')))
print(update_setting({'theme': 'light'}, ('theme', 'dark')))
print(update_setting({'volume': 'low'}, ('volume', 'high')))
print(delete_setting({'theme': 'light'}, 'theme'))
print(view_settings({}))
print(view_settings(test_settings))

    